<h1>シートに使用する調査項目の追加</h1>
<p style="color: green"><%= flash[:notice] %></p>

<%= render :partial => "form_group" %>

<%= link_to 'キャンセル', group_survey_sheet_sheet_properties_path(@group, @survey, @sheet) %>
